export const BUILD_VERSION = __BUILD_VERSION__

export const BUILD_METADATA = __BUILD_METADATA__

export const BUILD_PUBLIC_VERSION_LABEL = `v${BUILD_METADATA.version} · commit ${BUILD_METADATA.commitShort} · deploy ${BUILD_METADATA.deployShort}`
